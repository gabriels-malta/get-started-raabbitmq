# [RabbitMQ](https://www.rabbitmq.com/) Tutorials

This repository provides some basic use cases involving message broke approaches.

RabbitMQ is a lightweight message broker that supports multiple messaging protocols.

## Pre-Requisites
- Docker running
- The .net5 SDK, to execute all projects

## Usage
- Execute the file 'rabbitmq-server.ps1' to start up the docker image.
- Choose one use case, and execute the file 'showcase.ps1'
